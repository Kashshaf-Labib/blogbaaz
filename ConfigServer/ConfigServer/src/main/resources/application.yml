server:
  port: 8083
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kashshaf-Labib/microservice-config-servers
          clone-on-start: true
